# ============================================================
#  AirdropAgent — Dépendances Python
# ============================================================

# LLM
groq>=0.9.0                  # Backend LLM cloud (GitHub Actions)
ollama>=0.2.0                # Backend LLM local (Termux) — optionnel

# Agent & Orchestration
pyyaml>=6.0.1                # Lecture settings.yaml
requests>=2.31.0             # HTTP général
httpx>=0.27.0                # HTTP async (futur)

# Scraping Twitter (Nitter)
beautifulsoup4>=4.12.0       # Parsing HTML Nitter
lxml>=5.0.0                  # Parser HTML rapide

# Telegram
telethon>=1.36.0             # Client Telegram API officielle

# CLI & Display
typer>=0.12.0                # CLI moderne
rich>=13.7.0                 # Affichage terminal coloré

# Base de données
# sqlite3 est inclus dans Python standard — pas de dépendance externe

# Utilitaires
python-dateutil>=2.9.0       # Parsing de dates
